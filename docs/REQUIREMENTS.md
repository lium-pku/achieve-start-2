# 时间小达人 · 需求文档

> 面向小学生的家庭时间管理应用，支持孩子/妈妈/爸爸三种角色协同。
> 本文档是产品的**单一事实来源**，所有功能开发与测试用例均以此为准。

---

## 一、产品概述

### 1.1 目标用户
- 小学生（主要使用者，打卡执行任务）
- 家长（管理活动、审核积分、代打卡、复盘点评）

### 1.2 核心价值
- 培养孩子时间管理能力
- 通过积分机制激励按时完成任务
- 家长可代打卡（解决孩子没手机的问题）
- 积分审核机制保证公平
- 长期目标 + 短期复盘形成闭环

### 1.3 技术栈
- Next.js 16 + TypeScript + Tailwind CSS 4 + shadcn/ui
- Prisma ORM + SQLite（v1.x）/ PostgreSQL（v2.0+）
- Zustand（状态管理）
- Playwright（端到端测试）

---

## 二、角色与权限

### 2.1 三种角色

| 角色 | 标识 | 主要职责 |
|------|------|---------|
| 孩子 | `child` | 打卡完成任务、提交兑换申请、查看复盘 |
| 妈妈 | `mom` | 增删活动/奖励/成员、代打卡、审核打卡、审核兑换、写点评 |
| 爸爸 | `dad` | 同妈妈 |

### 2.2 权限矩阵

| 操作 | 孩子 | 家长 |
|------|------|------|
| 打卡自己的任务 | ✅ | ✅（代打卡） |
| 创建/编辑/删除活动 | ❌ | ✅ |
| 创建/编辑/删除奖励 | ❌ | ✅ |
| 创建/编辑/删除成员 | ❌ | ✅ |
| 审核打卡记录 | ❌ | ✅ |
| 审核兑换申请 | ❌ | ✅ |
| 触发扣分检查 | ❌ | ✅ |
| 兑换奖励 | ✅ | ❌ |
| 写复盘点评 | ✅（自评） | ✅ |
| 创建/编辑目标 | ✅（自己的） | ✅（代建） |
| 查看复盘统计 | ✅ | ✅（可查看任意成员） |

---

## 三、功能模块

### 3.1 日程管理

#### 3.1.1 四种周期
- **日度**（daily）：每天重复，需设置计划时间 + 截止时间
- **周度**（weekly）：每周指定星期几（1-7）执行
- **月度**（monthly）：每月指定日期（1-31）执行
- **临时**（once）：一次性活动，需指定具体日期（specificDate），只在该天出现

#### 3.1.2 活动字段
- 标题（必填）
- 描述（可选）
- 周期类型（日度/周度/月度/临时）
- 计划时间（HH:mm，可选）
- 截止时间（HH:mm，超过则不算按时，所有周期类型都可设，建议设置）
- 截止日期（endDate，可选，超过此日期活动自动停止，如学期末）
- 基础积分（完成即得）
- 按时奖励（截止时间前完成才得）
- 分配给孩子（支持多选：assignedToIds 数组）
- 具体日期（specificDate，仅临时类型必填）

#### 3.1.3 活动分配（多孩）
- **指定孩子**：活动可分配给一个或多个孩子（assignedToIds 逗号分隔）
- **公共活动**：assignedToIds 为空 = 所有孩子都需要打卡
- 公共活动每个孩子可独立打卡（各自的 ActivityLog）
- 非分配的孩子看不到该活动
- API 过滤：GET /api/activities?assignedToId=xxx 返回该孩子的活动 + 公共活动

#### 3.1.4 两种视图
- **列表视图**：按日度/周度/月度 Tab 分组，卡片式展示
- **时间网格视图**（默认）：纵向时间轴，活动条高度=时间跨度
  - 智能时间范围（根据当天活动自动收缩）
  - 重叠活动并排分栏
  - 当前时刻红色指示线
  - 家长可拖动条目上下调整时间（5 分钟对齐）
  - **日视图/周视图切换**：
    - 日视图：左右箭头换前一天/后一天，"回到今天"快捷按钮
    - 周视图：7 行紧凑俯览（周一到周日），每行显示该天活动清单
    - 周视图点击某天可切到日视图
  - **日视图 3-panel 轮播 + 预加载**（v2.2.0 新增）：
    - **3-panel 结构**：prev / current / next 三个面板横向排列，track 宽度 300%，默认 `translateX(-33.333%)` 显示中间
    - **预加载前后天**：进入日视图时用 `Promise.all` 并行拉取前一天/今天/后一天的活动 + 打卡日志，存到 `dayData` state
    - **滑动跟手**：`pointermove` 时 `translateX` 实时跟手（阻尼 0.6，比单纯反馈更跟手），用户滑动过程能直接看到相邻日期内容
    - **snap 切换**：松手后水平滑动超过 50px → snap 到相邻 panel（300ms 过渡动画）；不足阈值 → 回弹到中央
    - **无闪烁切换**：snap 后不立即重置 `swipeOffset`，等父组件新数据到达（`dataDate === selectedDate`）后再重置，因新 current 面板内容 = 旧 next/prev 内容（同一天），用户看到的内容无跳变
    - **滑动与拖拽冲突处理**：家长拖动活动条时（`dragState.moved === true`）取消滑动；`justDraggedRef` 防止拖拽结束后误触发点击
    - **垂直滚动不受影响**：`touch-action: pan-y` 让垂直滚动正常工作
    - **左右滑动方向**：左滑（手指从右向左）→ 下一天；右滑（手指从左向右）→ 前一天
    - **快速连续滑动**：今天 → 明天 → 后天，每次滑动都能成功切换（预加载保证数据已就绪）
    - **空状态**：当天无活动时显示"📭 当天没有安排活动"+ "← 左右滑动查看其他日期 →"提示
  - **不区分周期类型**：网格显示选中天的所有活动（日度+周度+月度+临时按"是否在该天活跃"过滤）
  - **家长成员选择**：顶部孩子头像按钮，切换看谁的活动（默认第一个孩子）

#### 3.1.5 业务规则
- 周度活动必须指定 dayOfWeek（1-7）
- 月度活动必须指定 dayOfMonth（1-31）
- 临时活动必须指定 specificDate
- 非法 scheduleType 应拒绝
- 删除活动为软删除（active=false）
- 所有周期类型都可设截止时间（deadline）
- 所有周期类型都可设截止日期（endDate）
- 超过截止日期的活动自动停止出现

### 3.2 打卡与积分

#### 3.2.1 打卡流程
1. 孩子自己打卡 **或** 家长代打卡（记录 operatorId）
2. 打卡后状态 = `pending_verification`（待审核），**不立即发积分**
3. 家长审核通过 → 状态 = `completed`，发放基础积分 + 按时奖励
4. 家长审核拒绝 → 状态 = `rejected`，不发积分
5. 拒绝后可重新打卡

#### 3.2.2 按时奖励逻辑
- 打卡时当前时间 ≤ deadline → `onTime=true`，bonusAwarded = onTimeBonus
- 打卡时当前时间 > deadline → `onTime=false`，bonusAwarded = 0
- 审核通过时按 onTime 字段发放

#### 3.2.3 重复打卡限制
- 同一活动同一周期（日度=当天/周度=当周/月度=当月）只能打卡一次
- 状态为 `pending_verification` 或 `completed` 时拒绝重复打卡
- 状态为 `rejected` 或 `missed` 时允许重新打卡

#### 3.2.4 扣分机制
- 家长触发"扣分检查" → 找出所有"今日该出现但未打卡且超时"的活动
- 每个未完成活动扣除其基础积分（负数）
- **积分扣到 0 为止，不会变负数**
- 已打卡（待审核/已通过/已拒绝）的活动不会被扣分

#### 3.2.5 积分流水
- 每次积分变动都写流水（PointTransaction）
- 类型：earn（完成）/ bonus（按时奖励）/ penalty（扣分）/ redeem（兑换）/ adjust（手动调整）
- 手动调整积分（PATCH member.totalPoints）也必须写 adjust 流水
- 流水按时间倒序查询

### 3.3 批量审核
- 家长首页可见"待审核打卡"面板（始终显示，含空状态）
- 支持全选/单选
- "全部通过"按钮：无需勾选即可一键通过所有
- "拒绝选中"按钮：需先勾选
- 审核后面板实时刷新
- 代打卡记录显示"代打卡"标签

### 3.4 兑换系统

#### 3.4.1 奖励字段
- 标题、描述、图标、所需积分、创建者

#### 3.4.2 兑换流程
1. 孩子提交兑换申请（积分足够才能提交）
2. 提交时立即扣除积分
3. 家长审核：通过 → approved → 标记已兑现 fulfilled
4. 家长拒绝 → rejected → **退还积分**
5. 已 fulfilled 的兑换再次操作不应退分（幂等）

#### 3.4.3 业务规则
- 积分不足时兑换应失败
- 兑换记录包含状态：pending / approved / rejected / fulfilled
- 只能用"已审核积分"（totalPoints 仅含已审核的）

### 3.5 鼓励里程碑
- 达到累计积分阈值自动解锁称号
- 默认阈值：20/50/100（v1.x 有 5 个，固定 seed 有 3 个）
- 字段：threshold、title、message、icon
- 家长可自定义新增
- 按threshold 升序展示

### 3.6 规划功能

#### 3.6.1 个人长期目标
- 字段：标题 + 完成状态 + 截止日期 + 描述 + 归属成员
- 完成状态：未开始 / 进行中 / 已达成
- 每个成员独立目标（妈妈/爸爸/孩子各有）
- 按状态分组展示，支持快速切换
- 过期目标显示"已过期"标签（未达成且 deadline 已过）
- 支持成员过滤（全部/各成员）
- CRUD 完整：新建/编辑/删除
- 默认状态为 not_started
- 截止日期可清空（设为 null）
- 编辑时不可改归属成员

#### 3.6.2 复盘（数据统计）
- 周报/月报切换
- 4 个数字卡片：
  - 任务完成率 = 已完成 / 总任务数
  - 按时完成率 = 按时完成 / 已完成
  - 净增积分 = 获得 - 扣除 - 兑换
  - 未完成/扣分数
- recharts 趋势折线图：最近 4 个周期
  - 三条线：完成率、按时率、净积分
  - 周报 label 格式 "M/D"，月报 label 格式 "X月"
- 时间导航：可查看历史周期（offset 0=当前, 1=上一个...）
- 家长可查看任意成员的统计
- 月报 periodStart 必须是月初（1 号）
- 周报 periodStart 是周一

#### 3.6.3 复盘点评
- 点评周期（针对本周/本月整体表现）
- 所有成员都能写点评（含孩子自评）
- 周报点评 / 月报点评分离
- 历史点评列表按时间倒序
- 每条点评显示：作者、角色、周期、日期、内容
- 支持编辑内容（PATCH）
- 点评返回 author 关联数据

### 3.7 家庭管理
- 成员 CRUD：新增/编辑/删除
- 成员字段：姓名、角色、头像、主题色
- 非法 role 应拒绝
- 角色切换（前端 Zustand 持久化 currentMemberId）
- 积分流水查看（按成员）

---

## 四、状态颜色体系（全局统一）

| 状态 | 颜色 | 说明 |
|------|------|------|
| 待打卡 | 橙色 | 未操作 |
| 待审核 | 黄色 | 已打卡待家长审核 |
| 已通过 | 绿色 | 审核通过，积分已发 |
| 已拒绝 | 红色 | 审核拒绝，无积分 |
| 已扣分 | 灰色 | 超时未完成被扣分 |
| 当前时刻 | 红色线 | 网格视图指示线 |

---

## 五、导航结构

### 5.1 底部 Tab（5 个）
1. 首页 - 今日待办 + 积分总览 + 审核面板 + **孩子切换**（家长视角多孩时）
2. 日程 - 日程管理（列表/网格双视图）+ **成员选择**（家长视角）
3. 奖励 - 兑换商店 + 审核记录
4. 规划 - 目标/复盘/点评 三个子 Tab
5. 家庭 - 成员管理 + 积分流水

### 5.2 角色切换
- 顶部右侧下拉切换当前角色
- 切换后持久化到 localStorage

---

## 六、测试要求

### 6.1 测试基础设施
- Playwright 端到端测试
- 每次测试前 `resetAndSeed` 重置为固定初始数据
- 固定 seed：4 成员(小宇+小苒+妈妈+爸爸，积分0) + 5 活动 + 3 鼓励 + 2 奖励 + 2 目标
- 活动截止时间统一 23:59，避免测试因运行时间不稳定
- **双层测试**（v2.2.0 强制）：API 层（`tests/helpers.ts`）+ UI 层（`tests/ui-helpers.ts`）必须同时覆盖

### 6.2 测试覆盖范围（47 个测试文件，~370 cases）

#### API 层测试（`tests/01-31-*.spec.ts`，~120 cases）

| 流程 | cases | 覆盖点 |
|------|-------|--------|
| 1. 打卡→审核→发分 | 2 | 含重复打卡拒绝 |
| 2. 拒绝审核→重新打卡 | 1 | rejected 状态允许重打 |
| 3. 扣分流程 | 2 | 已打卡不扣 + 超时扣分 |
| 4. 兑换流程 | 3 | 足够/不足/拒绝退还 |
| 5. 代打卡→审核 | 2 | operatorId 记录 |
| 6. 活动 CRUD | 4 | 含参数校验 |
| 7. 成员 CRUD + 鼓励 | 4 | 含非法 role |
| 8. 批量审核 + fulfilled | 3 | 含幂等 |
| 9. 按时奖励 + 扣分保护 | 4 | 含负数保护 |
| 10. 积分流水 + login 幂等 | 4 | 含 adjust 类型 |
| 11. 目标 CRUD | 4 | 含成员隔离 |
| 12. 点评 CRUD | 5 | 含周月分离 |
| 13. 统计准确性 | 5 | 含趋势 |
| 14. 目标边界 | 7 | 含日期清空 |
| 15. 点评边界 | 6 | 含 author 关联 |
| 16. 统计计算 | 8 | 含公式验证 |
| 17. 奖励 CRUD | 3 | 含 seed 验证 |
| 18. 周期活动今日出现 | 4 | 日/周/月度判断 |
| 19. 多家庭隔离 | 6 | 含 401/404 |
| 20. 角色权限校验 | 11 | 孩子不能/能的操作 |
| 21. 跨家庭越权访问 | 9 | 7 类资源防护 |
| 22. 业务机制补全 | 5 | 鼓励解锁/目标锁定等 |
| 23. 临时日程 + 网格改进 | 6 | once 类型 + date 查询 |
| 24. 临时日程打卡/审核链路 | 4 | 打卡/审核/重复/拒绝重打 |
| 25. 各周期 deadline + 按时奖励 | 6 | weekly/monthly/once 持久化 + bonus |
| 26. 多孩功能 | 5 | 公共活动 + 多孩分配 + 独立打卡 |
| 27. 首页孩子切换 | 4 | 家长切换看不同孩子 + 代打卡 |
| 28. 截止日期（endDate） | 5 | 持久化 + 未到正常 + 超过停止 + 清空 |
| 29. 规划角色隔离 | 3 | 孩子只看自己的目标/点评 |
| 30. seed 数据完整性 | 3 | 4 成员 + 2 目标 + 各自可见 |
| 31. 规划权限 | 4 | 目标/点评只有孩子能编辑 |
| 47. API 日期过滤边界 | 11 | ?date= 今天/明天/昨天/跨月/跨年/startDate/weekly/monthly/once/endDate/公共活动 |

#### UI 层测试（`tests/32-46-ui-*.spec.ts`，~250 cases，v2.2.0 新增）

| 文件 | cases | 覆盖点 |
|------|-------|--------|
| 32. 登录页交互 | 6 | 3 个快速登录按钮、回车登录、错误码、空码禁用 |
| 33. AppShell 导航 | 10 | 5 个 Tab 切换高亮、MemberSwitcher 下拉、角色权限 UI 差异、未登录回登录页 |
| 34. 首页 Tab 渲染 | 15 | 欢迎卡、统计数字、活动状态 Badge、打卡/代打卡流程、孩子切换胶囊、可代打卡徽章、积分里程碑 |
| 35. 日程 Tab 渲染 | 11 | 网格/列表切换、daily/weekly/monthly 子 Tab、编辑/删除按钮角色权限、新建对话框、删除确认 |
| 36. 奖励 Tab 渲染 | 11 | 积分卡片、兑换商店/审核记录 Tab、积分不足/立即兑换按钮、pending 数量徽章、家长审核按钮 |
| 37. 家庭 Tab 渲染 | 11 | 4 个成员卡片、当前徽章、积分流水、使用说明、添加/编辑成员对话框 |
| 38. 规划 Tab 子页 | 8 | 3 个子 Tab 切换、目标列表、状态徽章、孩子角色新建目标 |
| 39. 待审核面板 | 5 | 空状态、孩子不可见、家长通过/拒绝流程、勾选计数 |
| 40. 日视图左右滑动 | 11 | 默认今天、箭头切换、"回到今天"、左滑/右滑、小幅度不触发、轮播 3-panel 结构、预加载无 loading、连续滑动 |
| 41. Dialog 表单交互 | 27 | 5 个 dialog（活动/奖励/成员/目标/鼓励）的表单填写、校验、提交、编辑预填、取消、周期类型切换、图标选择、实时预览 |
| 42. 活动详情对话框 | 9 | 标题/时间/积分/状态徽章/编辑入口/角色权限/Esc 关闭/周期标签 |
| 43. 网格视图角色权限 | 13 | GripVertical 拖动手柄、可拖动提示、成员选择胶囊、扣分检查、图例 + 周视图（7 天列表/点击切日视图/高亮今天） |
| 44. 规划子 Tab CRUD | 19 | 目标（新建/状态切换/删除/分组/编辑权限）、复盘（图表/成员切换/周期导航/积分汇总）、点评（周报月报/写点评/发布/空内容禁用/孩子只看自己） |
| 45. 奖励完整兑换流程 | 6 | 兑换→通过→兑现、拒绝流程、积分扣减、pending 徽章、已兑现不可操作、孩子无操作按钮 |
| 46. 边缘场景 | 13 | 加载状态、未登录回登录页、token 失效、空数据不崩溃、空奖励/空流水/空目标、快速切换 Tab/成员、Esc 关闭、多家庭隔离、网络错误 |

### 6.3 测试覆盖判定（v2.2.0 强制）

**任何"用户可见的行为"都必须有 UI 测试，不能只靠 API 测试。**

- ❌ 只测 API `/api/activities/complete` 返回 `status: pending_verification` → 不够
- ✅ 还要测 UI：点"打卡"按钮后，活动卡片上出现"待审核" Badge，"打卡"按钮消失

- ❌ 只测 API `/api/redemptions` 创建成功 → 不够
- ✅ 还要测 UI：孩子点"立即兑换"后，"审核记录" Tab 出现 pending 数量徽章

详见 `CLAUDE.md` 的"测试覆盖检查清单"。

---

## 七、v1.x 已知限制（v2.0 已解决）

1. ~~单家庭~~：v2.0 引入 Family + familyId 隔离
2. ~~无认证~~：v2.0 JWT + middleware
3. SQLite：仍用 SQLite（schema 兼容 Pg，部署时切换）
4. ~~无角色校验~~：v2.0 requireParent 校验
5. ~~测试端点风险~~：v2.0 删除 /api/init 和 /api/test/reset
6. ~~积分非事务~~：v2.0 addPoints 用 $transaction

---

## 八、版本历史

| 版本 | 主要内容 |
|------|---------|
| v1.0.0 | 基础功能：日程/积分/兑换/三角色 |
| v1.1.0 | 时间网格视图（纵向 + 拖拽） |
| v1.2.0 | 家长代打卡 + 积分审核 + 批量审核 |
| v1.2.1 | Playwright 测试体系 |
| v1.3.0 | 规划功能（目标+复盘+点评）+ 64 cases |
| v1.3.1 | 固定 seed 测试数据 + 需求文档存档 |
| v2.0.0 | 多用户后端改造（Family/JWT/familyId 隔离）+ 102 cases |
| v2.0.1 | 网格视图日/周切换 + 临时日程 + 成员选择 + 截止日期 + 开发规则 |
| v2.0.2 | 多孩功能（多孩子分配 + 公共活动 + 首页孩子切换）|
| v2.1.0 | 多孩功能完善 + 规划权限调整（目标/点评仅孩子可编辑）+ 136 cases |
| v2.2.0 | 日程日视图 3-panel 轮播 + 前后天预加载 + UI 测试体系完善（16 个文件，~250 cases）|

---

## 九、验收标准（按需求点）

### 9.1 日程管理
- [x] 日度/周度/月度/临时四种周期独立管理
- [x] 列表视图 + 时间网格视图可切换
- [x] 时间网格为默认视图
- [x] 家长可拖动网格条目改时间
- [x] 周度必须指定 dayOfWeek，月度必须指定 dayOfMonth
- [x] 临时必须指定 specificDate
- [x] 非法 scheduleType 拒绝
- [x] 网格视图日视图/周视图切换
- [x] 日视图左右箭头换前一天/后一天
- [x] 周视图 7 行紧凑俯览，点击某天切日视图
- [x] 网格显示选中天所有类型活动（不区分周期）
- [x] 家长成员选择（切换看谁的活动）
- [x] 所有周期类型都可设截止时间（deadline）
- [x] 所有周期类型都可设截止日期（endDate）
- [x] 超过截止日期的活动自动停止

### 9.1.2 日视图轮播预加载（v2.2.0 新增）
- [x] 3-panel 轮播结构（prev / current / next）
- [x] 预加载前后天数据（Promise.all 并行拉取）
- [x] 滑动过程可见相邻日期内容（不是空白或 loading）
- [x] 滑动跟手（阻尼 0.6）
- [x] 50px 阈值 snap 切换（300ms 动画）
- [x] 不足阈值回弹
- [x] 无闪烁切换（dataDate 同步机制）
- [x] 滑动与家长拖拽活动条不冲突
- [x] 垂直滚动不受影响（touch-action: pan-y）
- [x] 左滑 → 下一天，右滑 → 前一天
- [x] 快速连续滑动（今天→明天→后天）
- [x] 空状态显示"左右滑动查看其他日期"提示
- [x] 日期切换后活动列表自动刷新（修复隐藏 bug）

### 9.1.1 多孩分配
- [x] 活动可分配给多个孩子（assignedToIds）
- [x] 公共活动（assignedToIds 为空）= 所有孩子
- [x] 公共活动每个孩子可独立打卡
- [x] 非分配的孩子看不到活动
- [x] 首页家长可切换看不同孩子的任务
- [x] 家长可给任意孩子代打卡

### 9.2 打卡与积分
- [x] 孩子自己打卡 + 家长代打卡
- [x] 打卡后待审核，不立即发积分
- [x] 审核通过发分，拒绝不发
- [x] 按时完成有额外奖励
- [x] 重复打卡拒绝
- [x] 拒绝后可重新打卡
- [x] 扣分不会让积分变负
- [x] 已打卡不被扣分

### 9.3 兑换系统
- [x] 积分不足拒绝兑换
- [x] 兑换立即扣分
- [x] 拒绝退还积分
- [x] fulfilled 幂等

### 9.4 规划
- [x] 目标 CRUD + 状态切换
- [x] 每个成员独立目标
- [x] 复盘 4 个数字卡片 + 趋势图
- [x] 点评所有成员可写
- [x] 周报/月报分离

### 9.5 测试
- [x] ~370 cases 全过（API ~120 + UI ~250）
- [x] 每次测试从固定 seed 开始
- [x] 不累积测试垃圾
- [x] **双层覆盖**（v2.2.0 强制）：每个功能点同时有 API + UI 测试
- [x] UI 测试覆盖所有 dialog 表单交互
- [x] UI 测试覆盖角色权限差异（家长 vs 孩子）
- [x] UI 测试覆盖边缘场景（token 失效/空状态/多家庭隔离/网络错误）
- [x] UI 测试覆盖滑动交互（PointerEvent dispatch）

### 9.6 多用户安全（v2.0 新增）
- [x] JWT 认证：未登录 401 / token 无效 401
- [x] 健康检查不需认证
- [x] 多家庭数据隔离（成员/目标/奖励/鼓励/点评/统计/流水）
- [x] 跨家庭越权访问返回 404（活动/打卡/兑换/目标/成员/统计/兑换审核）
- [x] 角色权限校验：孩子不能创建活动/奖励/成员/鼓励/审核/扣分
- [x] 孩子可以打卡/兑换/写点评/创建自己的目标
- [x] 积分事务化（addPoints 用 $transaction）
- [x] 删除危险端点（/api/init, /api/test/reset）

### 9.7 业务机制补全
- [x] 鼓励阈值达到后可解锁（积分正确累积）
- [x] 目标编辑时不能改归属成员
- [x] missed 状态后可重新打卡
- [x] 点评历史按时间倒序
- [x] 积分流水包含多种类型（adjust/redeem 等）
